# EE Resume Shop - Package

This package includes:
- A minimal Vite + React static storefront (src/App.jsx)
- Product files (PDFs) in /product_files
- package.json (use `npm install` then `npm run dev` to test locally)

Next steps:
1. Replace the placeholder URLs in src/App.jsx:
   - gumroadUrl
   - fiverrLink
   - koFiLink

2. Upload the PDFs in /product_files to Gumroad and set price/variants.
3. Deploy the site (Vercel recommended).

Good luck — if you want, I can edit links for you or deploy the site files.

